spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  ai:
    openai:
      api-key: ${AI_KEY}

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRE}